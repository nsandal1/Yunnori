import turtle
from graphics import *
import random

#Graphics for Yunnori
s=3
win = GraphWin("My Fart", 100*s, 100*s)
seq = [s*20, s*40, s*60, s*80]
def main(color):
    
    c = Circle(Point(random.choice(seq),random.choice(seq)), 10)
    c.setFill(color)
    c.draw(win)
    ##print(win.getMouse()) # pause for click in window
    ##win.close()

col = ['blue', 'green', 'red', 'yellow']
n=2
COL=col[:n]


for i in COL:
    main(i)

def moveTo(c, newPos):
    oldPos=c.getCenter()
    oldX, oldY = oldPos.getX(), oldPos.getY()
    newX, newY = newPos.getX(), newPos.getY()
    moveX = newX - oldX
    moveY = newY - oldY
    c.move(moveX, moveY)
    #return c


def main():
    myPoint=win.getMouse()
    myX, myY = myPoint.getX(), myPoint.getY()
    mc = Circle(myPoint, 10)
    mc.setFill(i)
    mc.draw(win)
    return mc

c=main()
for x in range(1,100):
    newPos = win.getMouse()
    moveTo(c, newPos)

#screen=turtle.Screen()
#image = "~/Desktop/Yunnori Board/Yun.gif"
#screen.addshape(image)
#turtle.shape(image)


#Player Characters


"""
class Player(object):
    stillPlaying=True
    def __init__(self, rolls=0, charac):
        self.rolls=rolls
        self.position=0

    def randnum(self, rolls, position):
        self.rolls=random.choice((-1, 1, 2, 3, 4, 5))
        self.position=self.position+self.rolls
        


class Node(object):
    def getposition(X, Y):
        player.getNode().getX()
        player.getNode().getY
"""
